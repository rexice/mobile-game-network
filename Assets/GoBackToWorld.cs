using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GoBackToWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        LoadManager.ExitFightScene();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
